# Project Status — Autonomous Multi-Agent Venture

**Purpose of this file:** a fast on-ramp for a new chat session. Read this + `docs/autonomous-agent-venture-plan.md` and you have full context — no need to re-explain the project from scratch.

## Where things stand

**Phase: Phase 1 built, pending Eric's full review.** Phase 0 (sandbox pilot) is complete — see below. Phase 1 (the governance/board dashboard, plan doc §7) has been built in Lovable: a private, single-owner-auth web app called **"Agent Command Center"** in Eric's "Benson Creatives" Lovable workspace (workspace_id `3k9ij40EbkQSRldq7UAq`, project_id `3fbdb974-f77b-4e82-ba01-c199807996c6`). Eric has provisioned the owner account and can log in. Editor: https://lovable.dev/projects/3fbdb974-f77b-4e82-ba01-c199807996c6 — Preview: https://id-preview--3fbdb974-f77b-4e82-ba01-c199807996c6.lovable.app

**What it has:** Dashboard home (pending count, recent activity, ledger totals), Proposals (submit form; Approve/Veto/Defer with decision notes; a vetoed proposal can only come back as a new proposal linked via `resubmission_of`, never a silent edit-and-resubmit), Ledger (hire/fire/spend/revenue entries, optional link to authorizing proposal, running totals), Activity (read-only, auto-written by DB triggers — no manual logging), Disputes/appeals (file + mark resolved), and **Agent Board** (added after Eric's first look at the plain build felt disconnected from the "agents living/working" feel from the source video — a dark, map/board-style view where each distinct agent, derived from real `submitted_by`/`actor` values already in the data, shows as a status-colored station card: amber = awaiting review, emerald glow = active in the last 24h, dimmed = idle. No fabricated agents or fake activity — honest empty state when there's nothing recorded, and it will stay sparse until real agents exist to populate it).

**Important, deliberately unchanged from the plan doc:** no live external agent connection exists yet. All data (proposals, ledger entries) is entered by hand through the app's own forms, standing in for what a real agent would submit via API once Phase 2 connects Abacus.AI. Nothing in this app can currently take a real-world action — it is purely the control surface, per Phase 1's scope.

**Standing commitment (Eric's condition on moving forward):** Eric approved moving toward Phase 2 on the condition that we circle back and push the Agent Board further toward the video's actual feel — today's static status-colored cards were a first step, not the finished version. Don't treat the current Agent Board as done; revisit it for more of a live/ambient "world" quality once there's real agent activity to visualize.

**Phase 2 status: approved in principle, on hold — blocked on Abacus.AI access.** Eric agreed to move to Phase 2, but when asked to choose between (a) using Claude Code agents to write real proposals straight into the live Supabase database behind the board (functional today, no new credentials, skips Abacus.AI for now) or (b) holding until Abacus.AI is properly wired in per the plan doc's original architecture, **Eric chose (b).** This session has no Abacus.AI connector or API credentials — `SearchMcpRegistry`/`ListConnectors` confirm no such connector exists in the registry or is installed. **Nothing has been built for Phase 2.** To unblock, Eric needs to either: provide an Abacus.AI API key/credentials for this session to integrate against, or check whether Abacus.AI offers an MCP server that could be added as a claude.ai connector. Until then, do not start writing Phase 2 code or guess at credentials/endpoints — get real access confirmed first.

**Phase 0 headline result** (full detail in `docs/phase-0-pilot-results.md`): a 3-stage worker/critic pipeline (idea generation → market/competition analysis → scoring/ranking), 6 sub-agent calls, entirely in-sandbox, no external calls, no spend. The worker/critic verification loop worked — all 3 critics found real, substantive flaws in their paired worker's output (not rubber-stamping), including one critic that surfaced genuine fragility in a ranking's weighting scheme rather than a simple factual error. Cost was real and cheap: ~264,700 tokens / roughly $2 total across the 6 agents (Claude Sonnet 5), ~9 minutes of combined sub-agent wall-clock time. The actual 5-idea evaluation output ranked CoachDesk #1, RepurposeIt a close #2 that wins outright under some reasonable alternative weightings.

**Source of truth:** `docs/autonomous-agent-venture-plan.md`, tracked in [PR #1](https://github.com/ericbenong1/ericbenong1/pull/1) on branch `claude/video-review-cqrxwj`. That file has the full architecture, governance model, and open questions — this file only adds operational notes that don't belong in the architecture doc itself.

## Key decisions so far

- **Governance:** a board-meeting model with Eric holding unconditional veto on any real spend, external action, or new recurring commitment. Full detail in the plan doc §5.
- **Sub-agent hiring/firing:** delegated to a budget envelope + Hiring Committee (doesn't need board approval), with Eric retaining a standing right to hire/fire directly at any time, independent of the meeting cadence.
- **Financial recordkeeping:** a Controller/Bookkeeper agent role logs revenue/expense events with tax-relevant metadata as they happen — organizes records for a real accountant, does not replace one.
- **Ambition:** full self-funding autonomy is the long-term goal, reached only through the phased roadmap (§7) — currently at Phase 0, nothing built yet.
- **Product direction:** open-ended — agents research and decide what to build/sell, not a fixed product line.

## Environment / access notes

- **This Claude Code sandbox cannot reach YouTube or Substack** (`natesnewsletter.substack.com`, including its `/feed` and individual post URLs) — blocked by this environment's network egress proxy, confirmed via direct test. GitHub is reachable. **Workaround in use:** Eric pastes article/transcript text directly — done twice now (Open Brain post, then 4 more full pieces: the 10-agent video transcript + 3 Nate B. Jones articles on token/context saving, multi-agent verification ("Ringer"/institutions), and model routing). This works well — keep using it.
- **This Claude Code sandbox also cannot reach `abacus.ai` or `api.abacus.ai`** — same network egress proxy restriction, confirmed via direct `curl` (`CONNECT tunnel failed, response 403`), independent of credentials. This is a hard blocker for Phase 2 as originally scoped (Claude Code calling Abacus.AI's API directly) — **no API key fixes this; it needs the Claude Code environment's network policy reconfigured (or a less restrictive environment created) before any Abacus.AI integration can work from this session.** See code.claude.com/docs/en/claude-code-on-the-web for how environment network policy is configured. Eric provided an Abacus.AI API key (`s2_0bce...`, redacted here — **never write the full key into this repo or any committed file**) on 2026-08-28 before this restriction was discovered; the key was not used in any authenticated call, so nothing was spent/exposed beyond Eric having pasted it in chat. Recommend Eric rotate that key as routine hygiene since it was shared in plaintext, whenever convenient.
- **Nate B. Jones** (newsletter, YouTube, GitHub `NateBJones-Projects`) is Eric's trusted source for AI engineering ideas/tools. Now grounded in primary-source content he's pasted directly (see above), not secondhand summaries — this substantially upgraded confidence in the plan doc's design in several places (see plan doc §2 update note, §5 appeals process, §6 routing/context techniques, §8 security note).
  - **Ringer** — a real, open-source local pre-call router + swarm orchestrator (per Nate's own July 2026 writeup, not just a GitHub summary now). Intercepts requests from **Codex CLI** specifically before they reach a provider — can return a cached accepted answer, run a fixed code path, select relevant source passages, or stop an oversized request. Worker lanes: Codex, Grok Build, OpenCode/OpenRouter. Ships with "Ringside," a browser dashboard. **This is why it needs a local machine (Eric's Windows laptop), not this cloud session** — it's CLI-based local software, not a hosted service. Important caveat, in Nate's own words: **Ringer does not yet reliably support Claude Code** (its Anthropic adapter passes unit tests but failed a live check as of July 2026) — so it's not a drop-in for this plan's Claude-Code-centric execution layer without further checking. Candidate to evaluate during Phase 0, not yet adopted.
  - **AI Airlock** (macOS-only document sanitizer for AI tools) — not usable for Eric, no macOS available.
  - **"Open Brain"** — resolved: Eric pasted Nate's Mar 2026 post in full. It's a personal, MCP-accessible knowledge store (Postgres + pgvector via Supabase, captured via Slack, ~$0.10–0.30/mo) that any AI tool can semantically search instead of Eric re-explaining context every new chat. This is **Eric's personal context layer, distinct from the venture's own agent-team memory** (the Abacus.AI RAG store in the plan doc). Noted as a candidate Phase 1+ integration in plan doc §4 (Chief of Staff querying it via MCP at board-meeting time) — not built, not committed, not blocking anything. The actual setup guide/prompt kit is paid content behind Nate's paywall — implementation steps aren't accessible from this environment; paste specific steps if/when we build it.
- **Devices:** Eric uses Chrome/Chromebook, the Claude app, and a Windows laptop (no macOS). This session runs in the cloud regardless of what device he's on — no device switch needed unless a specific future tool requires local execution (e.g., Ringer, confirmed above to be exactly that kind of tool). Nothing currently requires switching.
- **Platforms available:** Claude Code/API, Abacus.AI, Lovable — all already accessible. Gemini available as a routed option via Abacus.

## Session practice (to manage context/cost)

- The repo (this file + the plan doc) is the persistent record — not the chat history. A new session should be bootstrapped by pointing it here rather than re-explaining the project.
- Keep working in one session for active back-and-forth on a single topic. Start a **new session at real phase boundaries** (e.g., when Phase 0 execution actually kicks off) rather than per message.
- To bootstrap a new session: *"Read `docs/autonomous-agent-venture-plan.md` and `STATUS.md` in ericbenong1/ericbenong1 (branch `claude/video-review-cqrxwj`, PR #1), then continue from there."*
- Update this file (not just the plan doc) whenever an operational/environment fact changes — access constraints, tooling decisions, device notes — so it doesn't only live in chat history.

## Next concrete action

**Blocking item, revised: an API key alone doesn't unblock Phase 2 — network egress to abacus.ai is blocked at the environment level in this Claude Code sandbox** (see Environment/access notes above). Eric provided a key on 2026-08-28, but before it could be tested, direct connectivity tests confirmed this session cannot reach `abacus.ai`/`api.abacus.ai` at all. Two real options, both requiring an explicit choice from Eric — don't assume either without asking:
1. Reconfigure this Claude Code environment's network policy (or create a new environment) to allow Abacus.AI egress, then resume Phase 2 there.
2. Reconsider the Claude-Code-writes-directly-to-the-board approach Eric declined earlier — still available, doesn't need any Abacus.AI network access at all, though it forgoes Abacus.AI's RAG memory/RouteLLM cost routing per the original architecture.

Don't start writing Phase 2 integration code until Eric picks one of these.

Also still open, independent of the above: (1) the standing commitment above — Agent Board needs another visual pass to get closer to the video's feel, not yet done; (2) Phase 0's own output (the ranked 5-idea evaluation, CoachDesk #1) is still sitting unaddressed — no decision yet on whether to act on it at all.
